const MEETING = {
    PENDING: "pending", // applied meetings
    REJECT: "reject", // rejected meetings
    ACTIVE: "active", // upcoming meetings
    COMPLETE: "complete",
};

export default MEETING;
