// import express from "express";

// import { getRubricWeight, createRubricWeight, updateRubricWeight, deleteRubricWeight } from "../controllers/rubricWeight.js";

// const router = express.Router();
// import auth from "../middleware/auth.js";

// router.get("/", getRubricWeight);
// router.post("/", auth, createRubricWeight);
// router.patch("/:id", auth, updateRubricWeight);
// router.delete("/:id", auth, deleteRubricWeight);

// export default router;

//this done, just uncomment
